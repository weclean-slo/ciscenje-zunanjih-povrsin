# Read the Docs configuration file for Sphinx projects
# See https://docs.readthedocs.io/en/stable/config-file/v2.html for full options

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

# Optional Python requirements (uncomment if you use a requirements.txt)
# python:
#   install:
#     - requirements: docs/requirements.txt